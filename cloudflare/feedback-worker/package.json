{
  "name": "arxiv-daily-feedback-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply arxiv-daily-feedback --local",
    "db:migrate:remote": "wrangler d1 migrations apply arxiv-daily-feedback --remote",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "esbuild": "0.28.1",
    "miniflare": "5.20260907.0-alpha",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.1"
  }
}
